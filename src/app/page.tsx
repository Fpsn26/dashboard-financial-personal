"use client"

import { Transaction } from "@/types/transaction";

export default async function Page() {
    let test: Transaction;

    return (
        <div>
            <p></p>
        </div>
    );
}